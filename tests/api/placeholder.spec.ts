import { test } from '@playwright/test';

test.skip('API placeholder - replace with real tests', async () => {
  // This placeholder keeps the Playwright project healthy until real tests are added.
});
